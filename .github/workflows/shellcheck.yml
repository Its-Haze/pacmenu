name: ShellCheck

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ShellCheck
        run: sudo apt-get install -y shellcheck

      - name: Run ShellCheck
        run: |
          shellcheck bin/pacmenu bin/pacmenu-install bin/pacmenu-remove bin/pacmenu-search lib/pacmenu-core.sh install.sh

      - name: Syntax check (bash -n)
        run: |
          for f in bin/pacmenu bin/pacmenu-install bin/pacmenu-remove bin/pacmenu-search lib/pacmenu-core.sh install.sh; do
            echo "Checking $f..."
            bash -n "$f"
          done
